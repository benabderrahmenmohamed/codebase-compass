# --- Secrets: never, under any circumstances -------------------------------
.env
.env.*
!.env.example
*.key
*.pem

# --- Python ---------------------------------------------------------------
backend/venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/

# --- Node / frontend ------------------------------------------------------
frontend/node_modules/
frontend/dist/
frontend/.vite/
npm-debug.log*

# --- Analysis engine temp directories ---------------------------------------
analyse_qualite_*/

# --- The SQLite database ----------------------------------------------------
# Local data, not source. It holds submitted code, which may belong to
# someone other than whoever cloned the repository.
backend/data/
*.db

# --- Local tool configuration ---------------------------------------------
.claude/settings.local.json

# --- Personal documents ----------------------------------------------------
# The university report carries a real name, supervisors and an institution.
# This repository is public: that belongs on a hard drive, not in it.
*.docx

# --- OS and editors ----------------------------------------------------------
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
